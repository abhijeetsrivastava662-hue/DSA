1class Solution {
2public:
3    int longestSubarray(vector<int>& nums, int k) {
4        int maxi=0;
5        for(int i=0;i<nums.size();i++){
6            unordered_map<int,int> freq;
7            long long sum=0;
8            for(int j=i;j<nums.size();j++){
9                sum+=nums[j];
10                int val=(int)(((2LL*nums[j])%k+k)%k);
11                freq[val]++;
12
13                int rem=(int)((sum%k+k)%k);
14                if(rem==0 || freq.count(rem)){
15                    maxi=max(maxi,j-i+1);
16                }
17                }
18            }
19        return maxi;
20    }
21};